Log:
<div id="log">
<p>[Getting the log...]</p>
</div>

<script>
// super hacky ajax
function getLog(id){
  //get the list of tasks and rewrite the task
  $.getJSON(window.location.href + ".json", function(data) {
    document.getElementById("log").empty
    document.getElementById("log").innerHTML = "<span class='like-pre-dark'>" + data["log"] + "</span></br>";
  });
}

window.setInterval(function(){
  getLog(<%=@result.id%>);  //calling every 5 seconds
}, 5000);

//call right away
getLog(<%=@result.id%>);
</script>
