:verbose: false
#:logfile: ./log/task-autoscheduled.log  # this is commented bc we supply it on the cli
#:pidfile: ./tmp/pids/task-autoscheduled.pid # this is commented bc we supply it on the cli
:concurrency: 10
:queues:
  - task_autoscheduled
