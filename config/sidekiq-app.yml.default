:verbose: false
#:logfile: ./log/sidekiq-app.log # this is commented bc we supply it on the cli
#:pidfile: ./tmp/pids/app-worker.pid # this is commented bc we supply it on the cli
:concurrency: 10
:queues:
  - app
